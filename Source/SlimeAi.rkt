#lang racket
(require (file "classes.rkt"))

;(define (make_object slime opponent randomjump state)
 ; (define (move_logic)
  ;  (if (< (car (ball 'get_pos)) 500)
   ;     (set! state -1)
    ;;    (if (or (!= state -1) (and (= 0 (ball 'get_vel)) (= windowXbound (ball 'get_pos))))
      ;      (if (= state -1)
       ;         (if ( < 250 (car (opponent 'get_pos)))
        ;                (set! state 0)
         ;               (set! state 1))
          ;      (if (< (random) .35)
           ;         (set! state (truncate (* 2 (random))))
   ;                 'done))
            ;(cond [(and (= 0 state) (< (cdr (ball 'get_pos) 
                
                    
    
  ;(define (dispatch op)
   ; (cond ((eq? op 'move_logic) )                             
    ;      (else (error "Unknown op: " op))))
  ;dispatch)

                                      